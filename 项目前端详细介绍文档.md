# 校园打卡微信小程序前端详细介绍文档

## 一、项目概述

本项目是课堂打卡系统的微信小程序前端，采用微信小程序原生开发框架构建，实现了低耦合、易换肤、易维护的架构设计。项目提供学生、班委和教师三种角色的交互界面，完整实现了打卡管理、请假申请、班级管理等功能模块。

在架构设计层面，本项目采用了分层设计的思想，将配置层、网络层、样式层、业务层进行了清晰的分离。配置层集中管理 API 地址和接口定义，网络层统一封装了请求逻辑并提供了拦截器、重试、缓存等高级特性，样式层通过 CSS 变量实现主题管理，业务层则专注于页面逻辑的实现。这种分层架构的优势在于：当后端接口地址发生变化时，只需修改配置层文件；当需要换肤时，只需修改样式层文件；业务逻辑的调整则不会影响其他层次，真正实现了"改一处不动全身"的设计目标。

## 二、技术栈

本项目采用微信小程序原生技术栈进行开发，具体技术选型如下：

开发框架方面，使用微信小程序原生开发框架，这是微信官方提供的开发方式，具有性能好、兼容性强、生态完善等优势。开发语言采用 JavaScript，这是微信小程序原生支持的语言，也是前端开发最普及的语言。样式方面使用 WXSS，这是 CSS 的子集，语法与 CSS 基本一致，支持绝大部分 CSS 特性，同时增加了 rpx 单位以方便适配不同屏幕尺寸。页面结构使用 WXML，类似于 HTML 的标记语言，用于描述小程序页面的结构。

在架构层面，本项目还引入了模块化开发思想，通过 ES6 模块化语法实现代码的导入导出，提高了代码的可维护性和复用性。网络请求方面封装了统一的请求模块，提供了拦截器、自动 token 注入、请求重试、响应缓存等企业级特性。样式管理方面采用了 CSS 变量技术，实现了完整的主题切换功能。

## 三、目录结构详解

```
miniprogram/
├── app.js                    # 小程序应用入口文件
├── app.json                  # 小程序全局配置文件
├── app.wxss                 # 小程序全局样式文件
├── config/                   # 配置目录
│   └── api.js               # API 接口地址统一配置文件
├── network/                  # 网络请求层目录
│   ├── request.js           # 统一网络请求封装模块
│   └── api.js               # API 服务层模块
├── styles/                   # 样式目录
│   ├── theme.wxss           # 全局主题 CSS 变量文件
│   └── base.wxss            # 通用基础样式文件
├── utils/                    # 工具函数目录
│   ├── auth.js              # 认证相关工具（保留兼容）
│   ├── theme.js             # 主题管理器模块
│   └── utils.js             # 通用工具函数模块
└── pages/                   # 页面目录
    ├── login/               # 登录页面模块
    │   ├── login.js         # 登录页面逻辑
    │   ├── login.json       # 登录页面配置
    │   ├── login.wxml       # 登录页面结构
    │   └── login.wxss       # 登录页面样式
    ├── student/             # 学生相关页面模块
    │   ├── student.js       # 学生主页逻辑
    │   ├── student.json     # 学生主页配置
    │   ├── student.wxml     # 学生主页结构
    │   ├── student.wxss     # 学生主页样式
    │   ├── student-detail.js    # 学生详情页逻辑
    │   ├── student-detail.json  # 学生详情页配置
    │   ├── student-detail.wxml  # 学生详情页结构
    │   ├── student-detail.wxss  # 学生详情页样式
    │   ├── leave-apply.js       # 请假申请页逻辑
    │   ├── leave-apply.json     # 请假申请页配置
    │   ├── leave-apply.wxml     # 请假申请页结构
    │   ├── leave-apply.wxss     # 请假申请页样式
    │   ├── leave-records.js     # 请假记录页逻辑
    │   ├── leave-records.json   # 请假记录页配置
    │   ├── leave-records.wxml   # 请假记录页结构
    │   └── leave-records.wxss   # 请假记录页样式
    └── teacher/              # 教师页面模块
        ├── teacher.js       # 教师主页逻辑
        ├── teacher.json     # 教师主页配置
        ├── teacher.wxml     # 教师主页结构
        └── teacher.wxss     # 教师主页样式
```

## 四、核心模块详解

### 4.1 配置层（config/）

**config/api.js** 是整个项目的配置中心，集中管理所有与 API 相关的配置信息。这个文件的核心作用是实现接口地址的统一管理，使得在开发环境、生产环境之间切换时，只需要修改一处配置即可。

该文件主要包含三个部分：第一部分是环境配置，定义了 development（开发环境）和 production（生产环境）两种环境配置，每种环境包含 baseUrl（接口基础地址）和 timeout（请求超时时间）两个参数，当前默认使用开发环境，接口地址为 http://127.0.0.1:5000/api；第二部分是接口端点配置，通过 API_ENDPOINTS 对象统一管理所有接口路径，包括登录、学生打卡、请假申请、教师管理等各类接口的路径定义；第三部分是导出模块，将当前环境的配置、接口端点列表以及当前环境标识统一导出供其他模块使用。

通过这种配置方式，开发人员需要修改接口地址时，只需在文件顶部的 env 变量中切换环境，或者直接修改对应环境配置中的 baseUrl 值，整个小程序的所有接口请求都会自动使用新的地址，无需在业务代码中进行任何修改。

### 4.2 网络层（network/）

**network/request.js** 是项目的网络请求封装模块，提供了企业级的网络请求能力。该模块对微信原生的 wx.request 进行了封装，增加了多个高级特性。

首先，该模块实现了请求拦截器和响应拦截器功能，可以在请求发送前统一注入认证 Token，也可以在响应返回后统一处理错误码；其次，实现了自动 Token 注入功能，从本地存储中读取用户登录凭证并自动添加到请求头中；第三，提供了请求重试机制，当网络请求失败时，会自动重试最多 2 次，提高请求的成功率；第四，实现了请求缓存功能，对于需要缓存的请求可以将响应数据存储在内存中，下次请求时直接返回缓存数据；第五，提供了统一的错误处理，针对不同的 HTTP 状态码（如 401 未授权、403 无权限、500 服务器错误等）进行了统一处理，并自动跳转到登录页面；第六，实现了 Loading 自动管理，通过引用计数的方式自动显示和隐藏加载提示。

**network/api.js** 是项目的 API 服务层模块，是业务层与网络层的桥梁。该文件统一封装了所有业务接口的调用方法，包括认证相关（登录）、学生相关（打卡、记录查询、请假）、教师相关（班级管理、班委任命、请假审批）等模块。

这种设计模式的优势在于：业务层调用接口时，不需要知道接口的具体 URL 地址，也不需要处理请求的各项配置，只需要调用对应的方法并传入参数即可。当接口地址或请求方式发生变化时，只需要修改 api.js 文件中的实现，业务层代码无需任何改动。

### 4.3 样式层（styles/）

**styles/theme.wxss** 是本项目实现换肤功能的核心文件，通过 CSS 变量技术定义了完整的主题系统。该文件采用 CSS 自定义属性的方式，在 :root 根选择器下定义了一系列以双横线开头的变量，这些变量涵盖了项目中使用的所有颜色、间距、圆角、阴影等样式属性。

具体来看，theme.wxss 中定义的 CSS 变量包括以下几个类别：主色调变量（--primary-color、--primary-light、--primary-dark）定义了应用的主要颜色；辅助色变量（--secondary-color、--light-green）定义了次要颜色；背景色变量（--background-color、--background-gradient-*）定义了各种背景颜色和渐变起始结束颜色；文字颜色变量（--text-color、--text-secondary、--text-light、--text-white）定义了不同层级的文字颜色；组件颜色变量（--card-bg、--card-border、--input-bg、--input-border）定义了各种 UI 组件的背景和边框颜色；状态颜色变量（--success-color、--warning-color、--error-color、--info-color）定义了成功、警告、错误、提示等状态的颜色；阴影变量（--shadow-light、--shadow-medium、--shadow-heavy）定义了不同深度的阴影效果；动画变量（--transition-fast、--transition-normal、--transition-slow）定义了不同速度的过渡动画时长；圆角变量（--radius-small、--radius-medium、--radius-large、--radius-full）定义了不同大小的圆角值。

基于上述基础变量，theme.wxss 还定义了多个主题类（.theme-blue、.theme-purple、.theme-red、.theme-dark），每个主题类都重新定义了上述变量值，使得整个应用的配色风格发生改变。用户切换主题时，只需在页面容器上添加对应的主题类名，即可实现全局换肤效果，整个过程不需要修改任何业务代码。

**styles/base.wxss** 是项目的通用基础样式文件，定义了全局通用的样式类、动画和工具类。该文件包括全局页面容器样式、磨砂玻璃卡片效果、主按钮样式、次要按钮样式、输入框样式、标题样式等通用 UI 组件样式，以及 Flex 布局工具类、文字对齐工具类、间距工具类、圆角工具类等常用工具类。所有这些样式都使用 theme.wxss 中定义的 CSS 变量，从而实现与主题系统的无缝集成。

### 4.4 工具层（utils/）

**utils/theme.js** 是主题管理器模块，负责换肤功能的实现和主题状态的管理。该模块封装了 ThemeManager 类，提供了主题初始化、获取主题列表、设置主题、应用主题到当前页面、监听主题变化等功能。

具体功能包括：init 方法在应用启动时自动初始化主题，从本地存储读取上次保存的主题并应用到当前会话；getThemeList 方法返回所有可用的主题列表；getCurrentTheme 方法返回当前使用的主题 ID；setTheme 方法设置当前主题，自动保存到本地存储并触发主题变化通知；applyThemeToPage 方法将当前主题应用到指定页面；onThemeChange 方法注册主题变化监听器，当主题发生变化时自动调用注册的回调函数；showThemePicker 方法显示主题选择器，让用户可以通过底部操作菜单选择想要的主题。

通过这个模块，业务页面可以非常方便地接入主题功能，只需要在 onLoad 时调用 applyThemeToPage 方法，并在数据中添加 themeClass 字段，即可在页面渲染时自动应用当前主题。

**utils/utils.js** 是通用工具函数模块，封装了项目中常用的工具方法，包括：提示信息显示（showToast）、加载状态管理（showLoading/hideLoading）、对话框显示（showModal）、用户信息存取（getUserInfo/setUserInfo/clearUserInfo）、本地存储操作（getStorage/setStorage/removeStorage）、日期格式化（formatDate）、页面跳转（navigateTo/redirectTo/switchTab/navigateBack）、防抖节流函数（debounce/throttle）、深拷贝（deepClone）、手机号邮箱验证（isValidPhone/isValidEmail）等。

**utils/auth.js** 是保留的认证相关工具文件，主要用于兼容旧的代码调用方式，其中封装了与用户认证相关的基础方法。

### 4.5 应用入口（app.js/app.json/app.wxss）

**app.js** 是小程序的入口文件，在应用启动时首先执行。该文件当前主要完成两件事：一是调用 themeManager.init() 方法初始化主题系统，确保应用启动时能够正确加载上次使用的主题；二是定义 globalData 全局数据对象，用于存储全局共享的数据；三是将 themeManager 实例挂载到 app 对象上，方便各页面通过 getApp() 访问主题管理功能。

**app.json** 是小程序的全局配置文件，定义了小程序的所有页面路径、全局窗口配置（如导航栏颜色、标题等）、网络超时配置等。pages 数组列出了小程序的所有页面路径，window 对象配置了顶部导航栏的背景色、文字颜色和标题内容，networkTimeout 对象配置了网络请求的超时时间。

**app.wxss** 是小程序的全局样式文件，通过 @import 指令引入了 theme.wxss 和 base.wxss 两个样式文件，使主题变量和基础样式在整个小程序范围内生效。

### 4.6 页面模块（pages/）

**pages/login/** 是登录页面模块，包含用户登录所需的全部资源。login.js 文件负责处理用户输入、调用登录接口、处理登录结果等逻辑；login.wxml 文件定义了登录表单的界面结构，包括账号输入框、密码输入框、登录按钮等；login.wxss 文件定义了登录页面的样式，包括背景动画、表单样式、按钮样式等。

**pages/student/** 是学生相关页面模块，包含四个子页面：student.js 是学生主页，实现了打卡功能、查看打卡时间、跳转到请假申请和请假记录等功能；student-detail.js 是学生详情页，实现了个人打卡记录查询和班级打卡记录查询功能；leave-apply.js 是请假申请页，实现了请假日期选择、天数计算、请假申请提交等功能；leave-records.js 是请假记录页，实现了请假记录列表展示和下拉刷新功能。

**pages/teacher/** 是教师页面模块，包含教师管理班级的全部功能。teacher.js 实现了班级列表加载、班级切换、打卡记录展示、班委任命与移除、请假审批等功能。

## 五、核心特性说明

### 5.1 低耦合设计

本项目采用分层架构设计，实现了各层之间的低耦合。配置层、网络层、样式层、业务层各自独立，又通过明确的接口进行通信。具体表现为：业务层不直接调用 wx.request，而是调用 api.js 封装的方法；当网络请求逻辑变化时，只需要修改 network/ 目录下的文件；样式实现完全基于 CSS 变量，业务逻辑不需要关心样式细节；接口地址变化只需要修改 config/api.js 文件。

这种设计模式使得项目的各个部分可以独立开发和测试，也方便后续的功能扩展和维护。例如，如果需要更换网络请求库（如使用 Taro、uni-app 等框架），只需要保持 api.js 的接口不变，重新实现其底层方法即可。

### 5.2 换肤功能

本项目的换肤功能是其核心亮点之一，实现了"改皮肤不动业务代码"的设计目标。用户可以通过调用 themeManager.showThemePicker() 方法打开主题选择器，从默认绿色、蓝色海洋、紫色浪漫、红色热情、暗夜模式等多种主题中选择自己喜欢的样式。

换肤的实现原理是：CSS 变量定义了所有样式相关的值，每个主题类（.theme-blue、.theme-purple 等）都重新定义这些变量的值；当用户选择主题时，themeManager 将对应的主题类名添加到页面的根元素上；由于所有样式都引用了 CSS 变量，变量值的变化会自动反映到所有 UI 元素上。整个过程不需要修改任何业务代码，只需要添加或切换主题类名即可。

### 5.3 统一网络请求

本项目对网络请求进行了统一封装，提供了企业级的网络请求能力。request.js 模块实现了请求拦截、响应拦截、自动 Token 注入、请求重试、响应缓存、统一错误处理等功能。

请求拦截器可以在请求发送前统一添加认证 Token、统一设置请求头等；响应拦截器可以在响应返回后统一处理错误码、统一处理返回数据格式等；自动 Token 注入功能会从本地存储中读取用户登录凭证并自动添加到请求头中；请求重试机制会在网络请求失败时自动重试最多 2 次；响应缓存功能可以将请求结果缓存到内存中，对于重复请求直接返回缓存数据，提高响应速度；统一错误处理会对不同类型的错误进行分类处理，如 401 错误会自动跳转到登录页面。

### 5.4 接口地址统一配置

本项目实现了接口地址的集中管理，所有接口地址都定义在 config/api.js 文件中。当需要修改接口地址时，只需要修改该文件中的配置即可，无需在业务代码中进行任何修改。

该文件支持多环境配置，可以轻松在开发环境和生产环境之间切换。开发环境配置使用本地地址 http://127.0.0.1:5000/api，生产环境配置使用正式的服务器地址。切换环境只需要修改 env 变量的值即可。

## 六、快速开始指南

### 6.1 开发环境准备

首先，需要从微信公众平台下载并安装微信开发者工具，然后注册微信小程序开发者账号。安装完成后，打开微信开发者工具，选择"导入项目"，找到本项目的 miniprogram 目录进行导入。如果还没有 AppID，可以选择使用测试号进行开发。

### 6.2 配置后端地址

在启动项目之前，需要确保后端服务已经启动，并且接口地址配置正确。接口地址在 config/api.js 文件中配置，默认配置为开发环境，接口地址是 http://127.0.0.1:5000/api。如果后端服务运行在其他地址，需要修改该文件中的 baseUrl 配置。

### 6.3 运行项目

配置完成后，点击微信开发者工具中的"编译"按钮，即可在模拟器中预览小程序效果。也可以点击"预览"按钮，使用微信扫码在手机上进行真机调试。

### 6.4 切换主题

在学生主页和教师主页中，已添加了"换肤"功能入口。点击换肤按钮，会弹出主题选择列表，选择任意主题后，整个应用的配色风格会立即发生变化。

## 七、开发规范

### 7.1 代码规范

本项目遵循以下代码规范：使用 ES6 模块化语法进行代码组织；变量和函数命名使用有意义的英文单词组合；页面逻辑与 UI 分离，JS 文件中不包含样式代码；WXML 文件中不包含复杂的逻辑代码；添加必要的注释说明代码功能。

### 7.2 样式规范

样式编写遵循以下规范：优先使用 CSS 变量而非硬编码的颜色值；保持样式的一致性，相同类型的元素使用相同的样式类；使用工具类实现常用的样式组合，减少代码重复；响应式设计适配不同屏幕尺寸。

### 7.3 数据交互规范

数据交互遵循以下规范：所有接口调用通过 api.js 封装的方法进行；网络请求显示加载状态；网络错误显示友好的错误提示；敏感数据存储在本地时进行加密处理。

## 八、注意事项

在开发过程中，需要注意以下事项：开发阶段需要在微信开发者工具中开启"不校验合法域名"选项，以便调试；上线发布前需要在微信公众平台配置合法域名；如果使用真机调试，需要确保手机与电脑在同一网络环境下；小程序包大小限制为 2MB，如果超出需要使用分包加载功能。

## 九、版本记录

本项目当前版本为 v2.0.0，实现了完整的低耦合、易换肤、易维护架构。主要更新内容包括：新增配置层（config/）实现接口地址统一管理；新增网络层（network/）实现统一网络请求封装；新增样式层（styles/）实现主题系统和通用样式；新增工具层（utils/）提供主题管理和通用工具函数；重构所有业务页面，接入新的架构体系。

初始版本 v1.0.0 实现了基本的打卡、请假、管理功能。
